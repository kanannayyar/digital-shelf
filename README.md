# digital-shelf
online library management system
# purpose 
ease in manual library tasks

use git add . to stage all files together

add -> stage -> commit
git commit -am "  "  (to add and commit together for small changes)
git push -u origin main (to set upstream once- all code after this can be pushed by git push command)
